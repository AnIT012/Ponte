"""エラーの辞書。check が出すコードごとに「何が起きたか・なぜ止めるか・どう直すか」。

ここが元で、`ponte explain E32`・仕様書 13章の表・ホームページのエラー一覧を作る。
checker.py が出すコードが全部ここにあることは tests/test_errors.py が確かめる。
"""
from __future__ import annotations

# (コード, 一言, なぜ止めるか, どう直すか)
ERRORS = [
    ("E01", "match に else が無い",
     "書いていない値が来た時に、何をするか決まっていないから。",
     "最後に `else -> 結果` を書く。宣言した状態を全部書いたなら else は要らない。"),
    ("E02", "until に上限が無い",
     "終わらないかもしれない待ちは、アプリを止めてしまうから。",
     "`repeat ... until 〇〇 or 1 hour passed` のように、回数か時間の上限を書く。"),
    ("E03", "action に example が2つ未満",
     "例が1つだと、AIが書いた中身が本当に分かっているか確かめられないから。",
     "当たる例と、当たらない例（`-> missing` など）を最低1つずつ書く。"),
    ("E04", "action に else が無い",
     "AIの中身が答えを出せなかった時の逃げ道が要るから。",
     "`else skip` / `else ask user` のように、困った時の動きを書く。"),
    ("E05", "tbd が残っている",
     "まだ決めていないことがあるまま動かすと、AIが勝手に決めてしまうから。",
     "決めて tbd を消す。決められないなら、そのパーツを使わない形にする。"),
    ("E06", "`##` が残っている",
     "`##` は「ここは止めて」という印。書いた人がまだ引っかかっているから。",
     "中身を確かめて、`##` を消すか `#`（ただのコメント）にする。"),
    ("E07", "when に状態を書いている",
     "「期限が近い」のような状態は、いつ起きるかが決まっていないから。",
     "when は出来事（`every day at 8:00` / `user taps ...`）にして、状態は where か list で絞る。"),
    ("E08", "move の対象が絞れていない",
     "どの1件を動かすか分からないまま、まとめて動いてしまうから。",
     "`move this to 状態` にするか、list で1件に絞る。"),
    ("E10", "ぶつかる move の勝ちが決まっていない",
     "同じ1件に違う move が同時に来た時、どちらが勝つか分からないから。",
     "flow に `failed > passed` のように勝ちを書く。"),
    ("E11", "list がぐるっと自分を指している",
     "A の元が B で B の元が A だと、中身が決まらないから。",
     "どちらかを thing から直接絞る形にする。"),
    ("E12", "入れ子",
     "入れ子は読む人にもAIにも間違えやすいから。1段で書く決まり。",
     "中の方に名前を付けて外に出し、その名前を使う。"),
    ("E13", "action の約束が食い違う",
     "example の答えや never が out の形に合っていないと、確かめようがないから。",
     "out に書いた形（`found monthday | missing` など）で example と never を書き直す。"),
    ("E14", "relate の then / before が一周している",
     "A の後に B、B の後に A だと、終わらないから。",
     "どこかの then を外すか、条件付きの rule に分ける。"),
    ("E15", "relate の勝ちが両方向",
     "`A > B` と `B > A` が両方あると、どちらも勝てないから。",
     "どちらか一方だけ残す。"),
    ("E16", "before の前が起きない",
     "`A before B` なのに A が起きる道が無いと、B が永遠に起きないから。",
     "A のきっかけ（when）を書くか、before を外す。"),
    ("E17", "逃げ道が2つある",
     "action の else と relate の else が両方あると、どちらが動くか分からないから。",
     "どちらか1つにする。"),
    ("E18", "match に無い状態がある",
     "宣言に無い状態の枝は、打ち間違いのことが多いから。",
     "宣言の状態名に直すか、宣言に足す。"),
    ("E19", "who が無い thing がある",
     "書いていないことは誰もできない決まり。見ることもできないから。",
     "`who` に `user can see Thing where ...` などを書く。"),
    ("E20", "他の箱を指す項目に gone が無い",
     "指している先が消えた時に、こちらをどうするか決まっていないから。",
     "`gone[remove too]` / `gone[leave empty]` / `gone[block]` のどれかを付ける。"),
    ("E21", "thing の形を変えたのに change が無い",
     "もう保存されているデータを、新しい形にどう合わせるか決まっていないから。",
     "`change Thing to v2` に、足した項目の初期値（`add memo text = ""`）や名前の付け替えを書く。"),
    ("E22", "ask ai に上限が無い",
     "AIへの問い合わせが止まらないと、お金と時間がかかり続けるから。",
     "how に時間かお金の上限（`limit 5 seconds` / `limit 1 yen`）を書く。"),
    ("E23", "connect の失敗の逃げ道が無い",
     "外のサービスはいつか失敗する。その時の動きが決まっていないから。",
     "relate に `rule名 else 〇〇` を書く。"),
    ("E24", "flow scene に無い画面の移動",
     "画面の行き来も flow と同じで、書いた道しか通れないから。",
     "`flow scene` に矢印を足すか、移動先を直す。"),
    ("E25", "words の訳が足りない",
     "片方の言語でだけ文字が抜けると、その言語の人に意味が伝わらないから。",
     "words ja / words en の両方に同じ名前で書く。1つの言語だけのアプリなら words を書かずに `\"文字\"` で直接書ける。"),
    ("E26", "名前の無いボタン・説明の無い画像",
     "読み上げを使う人に、何のボタンか伝わらないから（公開する時だけエラー）。",
     "ボタンに `named 名前`、画像に説明を付ける。"),
    ("E27", "通貨の違う money を足している",
     "円とドルをそのまま足すと、意味のない数になるから。",
     "同じ通貨どうしで足すか、別々に出す。"),
    ("E28", "定義されていない名前",
     "無い部品・無い list・無い項目・無いアイコンを指していると、動かしようがないから。",
     "エラーに出た名前を、定義されている名前に直すか、定義を足す。"),
    ("E29", "rule に do が2つ以上ある",
     "1つの rule は1つのことだけ。2つあると、片方だけ失敗した時にどうなるか分からないから。",
     "rule を2つに分けて、relate の `A then B` でつなぐ。"),
    ("E30", "通知の宛先が無い",
     "誰に知らせるかが決まっていないと、誰にも届かないから。",
     "`notify owner ...` のように、人を指す項目か me を書く。"),
    ("E31", "書き方が分からない（do・example の手順・読めない行）",
     "決まった形以外は、実行エンジンが動かせないから。example の手順の打ち間違いを見逃すと、確かめていないのに通ってしまうから。",
     "do は `ponte guide --rules` の表の形で。example の手順は given / adds / at / says / taps / gets / expect のどれか。"),
    ("E32", "型が合わない",
     "無い状態・無い項目・違う型の値を、動かす前に見つけるため。",
     "エラーに出た候補（「todo / done」など）から選んで直す。"),
    ("W09", "（注意）flow の状態に rule の抜けがある",
     "他の状態には rule があるのに、その状態の時の rule が無い。考え忘れかもしれない。",
     "その状態の時に何が起きるかを rule に書くか、何もしないなら気にしなくていい。"),
    ("W26", "（注意）名前の無いボタン・説明の無い画像",
     "試作中は注意だけ。公開する時（`--publish`）はエラーになる。",
     "ボタンに `named 名前`、画像に説明を付ける。"),
]

BY_CODE = {e[0]: e for e in ERRORS}


def explain(code: str) -> str | None:
    e = BY_CODE.get(code.upper())
    if e is None:
        return None
    c, title, why, fix = e
    return f"{c}  {title}\n\nなぜ止めるか: {why}\nどう直すか:   {fix}"


def errors_table() -> str:
    rows = ["| コード | 何が起きたか | なぜ止めるか | どう直すか |", "|---|---|---|---|"]
    esc = lambda x: x.replace("|", "\\|")            # 表の中の | は区切りにしない
    rows += [f"| {c} | {esc(t)} | {esc(w)} | {esc(f)} |" for c, t, w, f in ERRORS]
    return "\n".join(rows)
